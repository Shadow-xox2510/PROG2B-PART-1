# Database Design

The RaceDay database is designed using six related entities:

- Users
- Events
- Routes
- Categories
- EventEnrolments
- Results

Primary and foreign keys are used to maintain referential integrity between the tables.

The database also uses constraints such as NOT NULL, UNIQUE, DEFAULT and CHECK to improve data integrity.
# Category Endpoints

Event categories are managed using the following endpoints:

- GET /api/events/{eventId}/categories
- POST /api/events/{eventId}/categories
- PUT /api/events/{eventId}/categories/{categoryId}
- DELETE /api/events/{eventId}/categories/{categoryId}

Categories allow participants to select the appropriate race division when enrolling in an event.
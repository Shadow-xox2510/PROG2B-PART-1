# Event Endpoints

RaceDay provides endpoints for managing events.

- GET /api/events
- GET /api/events/{id}
- POST /api/events
- PUT /api/events/{id}
- DELETE /api/events/{id}

Organisers are responsible for creating, updating and deleting events.

Participants can browse available events.